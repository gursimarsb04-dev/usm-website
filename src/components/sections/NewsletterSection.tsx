import EmailCapture from '@/components/EmailCapture';

export function NewsletterSection() {
  return (
    <EmailCapture
      variant="banner"
      headline="Stay in the Loop"
      subtitle="Get updates on events, new SSA chapters, and ways to get involved."
    />
  );
}
